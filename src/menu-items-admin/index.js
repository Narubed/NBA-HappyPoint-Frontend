import contacts from './contacts';
import dashboard from './dashboard';
import reword from './reword';
import history from './history';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, contacts, reword, history]
};

export default menuItems;
